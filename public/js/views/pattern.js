// Generated by CoffeeScript 1.6.3
(function() {
  'use strict';
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['backbone', 'collections/pattern'], function(Backbone, PatternCollection) {
    var PatternView, _ref;
    return PatternView = (function(_super) {
      __extends(PatternView, _super);

      function PatternView() {
        _ref = PatternView.__super__.constructor.apply(this, arguments);
        return _ref;
      }

      PatternView.prototype.el = '.patterns';

      PatternView.prototype.initialize = function(options) {
        this.app = options.parent;
        return this.scaffold();
      };

      PatternView.prototype.scaffold = function() {
        var cols, html, i, rows, z;
        i = 0;
        cols = [];
        while (i < 13) {
          z = 0;
          cols[i] = $('<div class="col col-1">');
          rows = [];
          while (z < 16) {
            if (i === 0) {
              html = '<div data-bind="Sound.' + (z + 1) + '.name" class="slot slot-label">Sound ' + (z + 1) + '</div>';
            } else {
              html = '<div class="slot">&nbsp;</div>';
            }
            rows.push($(html));
            z++;
          }
          cols[i].append(rows);
          i++;
        }
        return this.$el.append(cols);
      };

      PatternView.prototype.render = function() {};

      return PatternView;

    })(Backbone.View);
  });

}).call(this);
